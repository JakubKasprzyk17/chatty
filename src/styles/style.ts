import { StyleSheet } from 'react-native';

const Style = StyleSheet.create({
  bigBorderRadius: {
    borderRadius: 24,
  },
  smallBorderRadius: {
    borderRadius: 12,
  },
});

export default Style;
